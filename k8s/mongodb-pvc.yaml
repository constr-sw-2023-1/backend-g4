apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-pvc
  namespace: cs-ns
spec:
  storageClassName: "oci"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi